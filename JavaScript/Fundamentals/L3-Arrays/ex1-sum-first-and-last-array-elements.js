function getSumOfFirstAndLastElement(numbers) {
    let firstElement = numbers[0];
    let lastElement = numbers[numbers.length - 1];
    console.log(firstElement + lastElement);
}

getSumOfFirstAndLastElement([1, 2, 3, 4, ]);
