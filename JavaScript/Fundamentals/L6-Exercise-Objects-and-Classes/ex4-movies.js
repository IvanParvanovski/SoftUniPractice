function movies(input) {
    let cinema = {};

    for (let movie of input) {
        let movieData = movie.split(' ');
        
        let command = movieData.shift();
        let movieName = movieData.join(' ');

       

    }
}